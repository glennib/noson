{
  "identifier": "review_score",
  "scope_class": "global",
  "description": "Reviewer's dice-roll score",
  "prompt": "An integer 1-6, or null when the article is not a review.",
  "model": "gemini-3.5-flash",
  "notes": "The schema allows either an integer between 1 and 6 (representing the dice roll) or a null value when the article is not a review.",
  "schema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "maximum": 6,
    "minimum": 1,
    "type": [
      "integer",
      "null"
    ]
  },
  "suspected_gaps": [
    "type(array)"
  ],
  "evaluation": {
    "samples": 1000,
    "generation_errors": 0,
    "invalid_samples": 0,
    "generation_error_examples": [],
    "invalid_examples": []
  }
}
