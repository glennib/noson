{
  "identifier": "locale",
  "scope_class": "site",
  "description": "Rendering locale override",
  "prompt": "A BCP 47-style code like nb or nb-NO.",
  "model": "gemini-3.5-flash",
  "notes": "The schema uses a string type with a pattern constraint matching standard language tags like 'nb' or 'nb-NO'. The regex is kept flexible regarding casing and subtag lengths to prevent valid variations from being erroneously rejected.",
  "schema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "description": "BCP 47-style locale code (e.g., 'nb' or 'nb-NO')",
    "pattern": "^[a-zA-Z]{2,3}(-[a-zA-Z0-9]{2,4})?$",
    "title": "Locale Override",
    "type": "string"
  },
  "suspected_gaps": [
    "pattern"
  ],
  "evaluation": {
    "samples": 1000,
    "generation_errors": 0,
    "invalid_samples": 0,
    "generation_error_examples": [],
    "invalid_examples": []
  }
}
