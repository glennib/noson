{
  "identifier": "author_slug",
  "scope_class": "global",
  "description": "Canonical author slug",
  "prompt": "Lowercase letters, digits and hyphens, like jane-doe-2.",
  "model": "gemini-3.5-flash",
  "notes": "A simple string type with a safe regular expression pattern enforcing lowercase alphanumeric characters and hyphens, preventing query-time failures from overly complex backtracking.",
  "schema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "description": "Canonical author slug consisting of lowercase alphanumeric characters and hyphens",
    "pattern": "^[a-z0-9-]+$",
    "type": "string"
  },
  "suspected_gaps": [
    "pattern"
  ],
  "evaluation": {
    "samples": 1000,
    "generation_errors": 0,
    "invalid_samples": 0,
    "generation_error_examples": [],
    "invalid_examples": []
  }
}
