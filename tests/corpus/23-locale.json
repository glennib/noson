{
  "identifier": "locale",
  "scope_class": "site",
  "description": "Rendering locale override",
  "prompt": "A BCP 47-style code like nb or nb-NO.",
  "model": "gemini-3.5-flash",
  "notes": "This schema validates BCP 47 locale strings using a basic regular expression that matches a 2-3 letter language code followed by optional subtags. It uses a loose range of lengths to prevent validation failures on valid extended locales.",
  "schema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "description": "BCP 47-style locale identifier",
    "maxLength": 20,
    "minLength": 2,
    "pattern": "^[a-zA-Z]{2,3}(-[a-zA-Z0-9]{2,8})*$",
    "type": "string"
  },
  "suspected_gaps": [
    "pattern"
  ],
  "evaluation": {
    "samples": 1000,
    "generation_errors": 0,
    "invalid_samples": 0,
    "generation_error_examples": [],
    "invalid_examples": []
  }
}
